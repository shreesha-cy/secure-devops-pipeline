apiVersion: v1
kind: Pod
metadata:
  name: test
spec:
  containers:
    - name: app
      image: docker.io/library/nginx
      securityContext:
        runAsUser: 1000
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"
